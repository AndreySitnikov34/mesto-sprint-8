export class Card {
	constructor({ params, userId, cardSelector, handleLikeCard, handleImageOpen, deleteCard, openCardDeletePopup }) {
		this._name = params.name;
		this._link = params.link;
		
		
	}
}